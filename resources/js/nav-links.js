/**
 * Header nav hover is CSS-only (Damai tempo).
 * Slide uses --lum-animation-smooth; no JS snap-reset between links.
 */
export function initNavLinks() {
    // no-op — kept for app.js import stability
}
